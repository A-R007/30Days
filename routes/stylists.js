const express = require("express");
const router = express.Router();
const Stylist = require("../models/stylist");

router.get("/", async (req, res) => {});

router.get("/:stylistId", async (req, res) => {});

router.get("/:stylistId/availability", async (req, res) => {});
